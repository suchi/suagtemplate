# ja/ — コマンド・スキルの日本語訳

`.claude/commands/`と`.claude/skills/`は英語がベース(正)であり、ここには参照用の日本語訳を置く。

`commands/`内に`commit_en.md`のようなファイルを置くと`/commit_en`という重複スラッシュコマンドとして登録されてしまうため、訳をこのフォルダに分離している。

英語側を変更したときは、この訳もあわせて更新する(ベスト・エフォート)。
